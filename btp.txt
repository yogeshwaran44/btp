reffered from: https://cap.cloud.sap/docs/

an entity is a structured data model that represents a business object, similar to a table in a database. It defines fields, their data types, and relationships with other entities using CDS (Core Data Services). These entities are used to generate database tables and expose data through services like OData.

a type is a reusable data definition used to define the structure or data type of fields inside entities. It helps avoid repetition and improves consistency across the data model.

an aspect is a reusable model fragment that can be applied to multiple entities to add common fields or behaviour. It helps avoid duplication and keeps the data model clean and consistent.

a service is used to expose entities as APIs so that external applications can access and perform operations like create, read, update, and delete (CRUD). Services are typically exposed as OData or REST endpoints.

an association is used to define a relationship between two entities. It represents a link (like a foreign key) without embedding the related data.

a composition is a strong relationship between two entities where the child entity is fully dependent on the parent. If the parent is deleted, the child is also automatically deleted.

Projection is a way to create a view of an existing entity by selecting specific fields or reshaping data, usually for exposing it through a service.

CAP is a framework provided by SAP to build enterprise applications using a model-driven approach, supporting data modelling, service creation, and business logic with minimal code. Full-stack framework  Supports Java & Node.js  Auto-generates OData/REST APIs

CDS is a data modelling framework in CAP used to define entities, relationships, and services in a structured and database-agnostic way.

CDL is the textual language used to write CDS models, typically stored in .cds files.

Arrayed types in CDS are used to store multiple values of the same type in a single field, similar to a list or array.

OData (Open Data Protocol) is a standard protocol used to build and consume RESTful APIs for querying and manipulating data.
OData = a standard way to access data over HTTP  Read data  Filter data  Sort data  Expand relationships  All using URL-based queries

An OData adapter is a component that translates application data and requests into the OData protocol format, enabling standardized data access and communication.

A namespace in CDS is used to logically group and uniquely identify entities, preventing naming conflicts across different models.

Annotations in CDS are metadata used to add additional information or behaviour to entities, fields, or services without changing the core data model.


An enum (enumeration) in CDS is used to define a fixed set of constant values that a field can take.


before handler : 
this.before(event, entity?, handler)
handler = async (req) => { }


on handler:
this.on(event, entity?, handler)
handler = async (req, next) => { }


after handler:
this.after(event, entity?, handler)
handler = async (data, req) => { }


cds object contains:
[
  '_events',            '_eventsCount', '_maxListeners',       
  'model',              'db',           'cli',
  'root',               'services',     'extend',
  'home',               'watched',      'version',
  'parse',              'utils',        'options',
  'env',                'plugins',      'requires',
  'server',             'log',          'builtin',
  'service',            'debug',        'app',
  'resolve',            'load',         'compile',
  'deploy',             'edmxs',        'minify',
  'type',               'linked',       'entity',
  'connect',            'Service',      'infer',
  'ql',                 'compiler',     'EventContext',        
  'User',               'serve',        'i18n',
  'ApplicationService', 'middlewares',  'Request',
  'shutdown',           'odata'
]

cds is not just one thing, it’s runtime engine, ORM, query builder, service framework.
@sap/cds is built on top of Express.js


types of auth:
basic
digest
api keys
session-based
jwt
oauth2
openid
single sign on


CDS artifacts were converted to hdbtable and hdbview artifacts. You will find those artifacts in a new folder under /gen/db/src called gen.
deploy those objects into the HANA Database creating tables and views

General Purpose
@title			          Human-readable label	@Common.Label
@description		          Detailed description	@Core.Description

Access Control
@restrict		          Defines fine-grained access rules
@requires		          Restricts access to specific roles

Input Validation
@readonly		          Field cannot be modified
@mandatory		          Field must have a value
@assert.target		          Ensures valid reference (association exists)
@assert.format		          Validates value using regex
@assert.range		          Restricts value within a range

Services / APIs
@path			          Defines custom API endpoint path
@impl			          Links to service implementation
@odata.etag		          Enables optimistic locking
@cds.autoexpose		          Automatically exposes entity in service
@cds.api.ignore		          Hides entity from API
@cds.query.limit	          Limits query result size
@cds.localized		          Enables multilingual support
@cds.valid.from/to	          Handles temporal (time-based) data
@cds.search		          Enables search functionality

Persistence
@cds.persistence.exists	          Table already exists (skip creation)
@cds.persistence.table	          Force creation as table
@cds.persistence.skip	          Skip DB persistence
@cds.persistence.mock	          Exclude from mock data
@cds.on.insert		          Auto-fill value on insert
@cds.on.update		          Auto-fill value on update
@sql.prepend		          Add SQL before generated SQL
@sql.append		          Add SQL after generated SQL

OData (Shortcuts)
@ValueList.entity	          Provides value help (dropdown list)
@odata.Type		          Override OData data type
@odata.MaxLength	          Set max string length
@odata.Precision	          Define numeric precision
@odata.Scale	                  Define decimal scale
@odata.singleton	          Marks entity as single instance

OData (Core Annotations)
@Core.Computed		          Value is auto-calculated
@Core.Immutable		          Value cannot be changed after creation
@Core.MediaType		          Defines MIME type
@Core.IsMediaType	          Marks field as media type
@Core.IsUrl		          Field contains a URL
@Capabilities.*		          Defines API capabilities (filter, sort)



@Semantics.currencyCode
@Semantics.unitOfMeasure
@Aggregation.CustomAggregate
@Semantics.amount.currencyCode
@Semantics.quantity.unitOfMeasure
@Aggregation.default
@Aggregation.CustomAggregate
@open
@odata.singleton.nullable







Informational responses (100 – 199)
Successful responses (200 – 299)
Redirection messages (300 – 399)
Client error responses (400 – 499)
Server error responses (500 – 599)

Informational responses
100 Continue
101 Switching Protocols
102 Processing Deprecated
103 Early Hints

Successful responses
200 OK
201 Created
202 Accepted
203 Non-Authoritative Information
204 No Content
205 Reset Content
206 Partial Content
207 Multi-Status (WebDAV)
208 Already Reported (WebDAV)
226 IM Used (HTTP Delta encoding)

Redirection messages
300 Multiple Choices
301 Moved Permanently
302 Found
303 See Other
304 Not Modified
305 Use Proxy Deprecated
306 unused
307 Temporary Redirect
308 Permanent Redirect

Client error responses
400 Bad Request
401 Unauthorized
402 Payment Required
403 Forbidden
404 Not Found
405 Method Not Allowed
406 Not Acceptable
407 Proxy Authentication Required
408 Request Timeout
409 Conflict
410 Gone
411 Length Required
412 Precondition Failed
413 Content Too Large
414 URI Too Long
415 Unsupported Media Type
416 Range Not Satisfiable
417 Expectation Failed
418 I'm a teapot
421 Misdirected Request
422 Unprocessable Content (WebDAV)
423 Locked (WebDAV)
424 Failed Dependency (WebDAV)
425 Too Early Experimental
426 Upgrade Required
428 Precondition Required
429 Too Many Requests
431 Request Header Fields Too Large
451 Unavailable For Legal Reasons

Server error responses
500 Internal Server Error
501 Not Implemented
502 Bad Gateway
503 Service Unavailable
504 Gateway Timeout
505 HTTP Version Not Supported
506 Variant Also Negotiates
507 Insufficient Storage (WebDAV)
508 Loop Detected (WebDAV)
510 Not Extended
511 Network Authentication Required




To implement domain-specific custom logic:

    Define services in CDS
    Add service implementations → cds.Service > Implementations
    Register custom event handlers in which → srv.on/before/after
    Read/write data from other services in which → srv.run + cds.ql
    ..., that is from your primary database → cds.DatabaseService
    ..., that is from other connected services → cds.RemoteService
    Emit and handle asynchronous events → cds.MessagingService


Node.js:
The cds Facade
cds. compile()
cds. linked()
cds. server
cds. serve()
cds. connect()
cds. Services
cds. Events
cds. Queries
cds. log()
cds. i18n
cds. env
cds. utils
Serving Fiori UIs
Transactions
Security
Plugins
Testing

Services:
In CAP, everything is a service.Expose data (Provided services),Consume external systems (Required services),Handle requests via event-driven architecture
Provided Services :
A CAP application mainly consists of the services it provides to clients. Such provided services are commonly declared through service definitions in CDS, and served automatically during bootstrapping
Required Services :
Required services are external services that an application depends on to function. A common example is the primary database like cds.db. Applications may also use services from other enterprise apps, microservices, or platform services such as secondary databases and message brokers.


CAP services work using event handlers:
before → validation
on → core logic (CRUD, actions)
after → modify response

Sending Requests:
srv.send() → REST-style
srv.run() → Query-based 

Events (Async)
srv.emit('EventName', data)
srv.on('EventName', msg => {})
Used for messaging & microservices

Transactions
srv.run(tx => { ... })
Consistency across DB actions




cds.Request → synchronous (CRUD/API)
cds.Event → asynchronous (messaging)


cds.Request (Synchronous Requests):
Extends cds.Event
Used for CRUD & actions
Key properties:
method → HTTP method
target → entity
params → URL params
query → CQN query
subject → single row reference

Return result:
return data  or req.reply(data)


Reject request (errors):
req.reject(400, 'MISSING_INPUT', 'title') - Sends proper HTTP error response
Collect multiple errors:
req.error(400, 'Invalid') -Stored in req.errors.Auto-rejected later

Messages (non-error):
req.info('Info')
req.warn('Warning')
req.notify('Message')
Sent via response headers

Request Data Access:
req.data → input data
req.query → query object
req.params → path parameters

Modify input safely:
req.data.field = 'value

Error Handling:
Errors converted to standard HTTP/OData responses
Supports:status,code,message,target
Example response:
{
  "error": {
    "code": "MISSING_INPUT",
    "message": "Input is required",
    "target": "title"
  }
}






cds.ql:
cds.ql is used to build queries in CQN (Core Query Notation)
Works like SQL but Database-agnostic
Can run on DB, OData, REST, etc.

Executing Queries:
Using service:  await srv.run(query)
Shortcut:  await SELECT.from(Books)
Default execution: Goes to cds.db

Queries are First-Class Objects
Queries are not executed immediately
Can be:Stored,Modified,Passed around
let q = SELECT.from(Books)
await srv.run(q)

Safe Queries (SQL Injection Protection):
CAP automatically: Uses binding parameters,Prevents SQL injection
Safe: SELECT.from`Books`.where`ID=${input}`

this.entities: → inside services
Belongs to a service instance

cds.entities: → global access
Global access to all entities in the model

SELECT
    .one
    .elements
    .distinct
    columns()
    from()
    alias()
    where()
    having()
    groupBy()
    orderBy()
    limit()
    forUpdate()
    forShareLock()
    hints()
    pipeline()
    stream()
    foreach()

INSERT

    into()
    entries()
    values()
    rows()
    from()

UPSERT

    into()
    entries()

UPDATE

    entity()
    set()
    with()
    where()

DELETE

    from()
    where()



CAP automatically manages transactions, so developers usually don’t need to handle them manually.
ACID transactions
User/tenant context
Connection pooling

Automatic Transactions (Default)
Every service call runs in a transaction
BEGIN → QUERY → COMMIT

Nested Transactions:
When one service calls another:
  await db.update(...)
  await log.insert(...)
Root transaction
Nested transactions for each service
Not fully distributed (no global atomic guarantee)



Manual Transactions (cds.tx()) - Used when you want full control

Auto-managed style (recommended):
await cds.tx(async () => {
  await INSERT.into(Authors)
  await INSERT.into(Books)
})
Automatically: Commit if success, Rollback if error

Manual control (advanced):
const tx = cds.tx()
try {
  await tx.run(...)
  await tx.commit()
} catch (e) {
  await tx.rollback(e)
}
You must handle commit/rollback manually


srv.tx() (Service-based Transaction) Same as cds.tx() but tied to a service
await srv.tx(async tx => {
  await tx.run(...)
})

Transaction Object (tx) Returned by cds.tx() or srv.tx()
Supports:
tx.run() → execute queries
tx.commit() → commit
tx.rollback() → rollback
also Contains its own tx.context

Background Jobs (cds.spawn()):
Run tasks outside current transaction
cds.spawn(async () => {
  await SELECT.from('Outbox')
})
Runs in new transaction can run once (after) repeatedly (every)



OData is an OASIS standard that enhances plain REST with standardized system query options like $select, $expand, $filter, and others

Metadata is descriptive information that explains the structure, meaning, or properties of data.

OData Query Options
$select	
$count	
$expand	
$format	
Read $links	
$value	
$orderby
$top	
$skip	
$filter	
$inlinecount	
$skiptoken

Logical Operators
And
Or
Not

Comparison Operators
Eq
Ne
Gt
Ge
Lt
Le
Has
In

OData syntax:
/BaseURL/EntitySet?$queryOption=value

example:
/PurchaseOrder?$filter=PurchaseOrder eq '100045' and PurchaseOrderItem gt 65
/Employees?$filter=PurchaseOrder eq '100045' and (PurchaseOrderItem gt 65 or PurchaseOrderItem lt 10)






Draft Work:

@odata.draft.enabled

1. Create a Draft
POST /odata/v4/AdminService/Books

2. Edit the Draft
PATCH /odata/v4/AdminService/Books(ID=a11fb6f1-36ab-46ec-b00c-d379031e817a,IsActiveEntity=false)

3. Activate the Draft
POST /odata/v4/AdminService/Books(ID=a11fb6f1-36ab-46ec-b00c-d379031e817a,IsActiveEntity=false)/draftActivate



Kinds of Actions

Actions modify data in the server
Functions retrieve data
Unbound actions/functions are like plain unbound functions in JavaScript.
Bound actions/functions always receive the bound entity's primary key as implicit first argument, similar to this pointers in Java or JavaScript. The exception are bound actions to collections, which are bound against the collection and not a specific instance of the entity. An example use case are custom create actions for the SAP Fiori elements UI.




Application Router
The application router is the single point-of-entry for an application running in the Cloud Foundry environment on SAP BTP. The application router is used to serve static content, authenticate users, rewrite URLs, and forward or proxy requests to other micro services while propagating user information.





Temporal Data
SAP CAP provides built-in support for temporal data, which means storing and querying records based on time.
using annotations and aspects.














@insertonly 
@common.text
@assert.integrity
@assert.unique
@cds.persistence.name
@cds.persistence.journal
@cds.redirection.target
@cds.persistence.udf
@cds.dp.ordId
@assert.notNull
@HideFromDataInspector
@cds.rateLimiting
@cds.validate
@cds.replicate
@cds.persistence.calcview
@cds.doc
@cds.ambiguous 
@cds.sql.dialect



**Required annotations on external service:**

- `@cds.external`
- `@data.product`
- `@cds.dp.ordId` - ORD ID of the external Data Product

**Add external package to your project's `package.json` dependencies:**





psuedo elements:
$user: {
      elements: {
        id: String,
        locale: String, // deprecated
        tenant: String, // deprecated
      },
    },
    $now: Timestamp,
    $at: Timestamp,
    $from: Timestamp,
    $to: Timestamp,
    $locale: String,
    $tenant: String,
}


 cf --help
cf version 8.18.0+fad4bcb.2026-03-04, Cloud Foundry command line tool
Usage: cf [global options] command [arguments...] [command options]

Before getting started:
  config    login,l      target,t
  help,h    logout,lo

Application lifecycle:
  apps,a        run-task,rt    events
  push,p        logs           set-env,se
  start,st      ssh            create-app-manifest
  stop,sp       app            delete,d
  restart,rs    env,e          apply-manifest
  restage,rg    scale          revisions

Services integration:
  marketplace,m        create-user-provided-service,cups
  services,s           update-user-provided-service,uups
  create-service,cs    create-service-key,csk
  update-service       delete-service-key,dsk
  delete-service,ds    service-keys,sk
  service              service-key
  bind-service,bs      bind-route-service,brs
  unbind-service,us    unbind-route-service,urs

Route and domain management:
  routes,r        delete-route    create-private-domain,create-domain
  domains         map-route
  create-route    unmap-route

Space management:
  spaces            create-space,csp    set-space-role
  space-users       delete-space        unset-space-role
  apply-manifest

Org management:
  orgs,o       set-org-role
  org-users    unset-org-role

CLI plugin management:
  plugins           add-plugin-repo      repo-plugins
  install-plugin    list-plugin-repos

Commands offered by installed plugins:
  bg-deploy                    mta        purge-mta-config
  deploy                       mta-ops    rollback-mta
  download-mta-op-logs,dmol    mtas       undeploy

Global options:
  --help, -h                         Show help
  -v                                 Print API request diagnostics to stdout

TIP: Use 'cf help -a' to see all commands.



------------------------------------------ destination -------------------------------------------------------

Choose Create   From Scratch   Create.
Enter a destination name.
From the <Type> dropdown menu, choose HTTP.
The <Description> field is optional.
From the <Proxy Type> dropdown box, select Internet, OnPremise, or PrivateLink, depending on the connection type you want to provide for your application.

Specify the destination URL.
From the <Authentication> dropdown box, select the authentication type you want to use for the connection.
(Optional) For Internet and PrivateLink connections only: You can enter a trust store location and password, or use the default client trust store.
(Optional) For OnPremise connections only: If you are using more than one Cloud Connector for your subaccount, you must enter the <Location ID> of the target Cloud Connector.
(Optional) You can enter additional properties.
In the Additional Properties panel, choose Add Property.
Enter a key (name) or choose one from the dropdown menu and specify a value for the property. You can add as many properties as you need.
To delete a property, choose the Delete icon next to it.
Note
For a detailed description of specific properties for SAP Business Application Studio (formerly known as SAP Web IDE), see Connecting to External Systems.

When you are done, choose Create.


in project :
configure package.json and add destination details in it.

"cds": {
   "requires": {
      "cordinates": {
        "kind": "odata-v4",
        "credentials": {
          "destination": "map_api"
        }
      }    
   }
}


cf create-service destination lite  service_name 
cf create-service-key service_name key_name
cds bind -2 service_name:key_name

then cdsrc file will be automatically modified with destination details




--------------------------------------------- xsuaa --------------------------------------------------------
cds add xsuaa 

cds add approuter 

 
npm install 

 
add this in xs security json 

"xsappname":"zentra_xsuaa", 

"tenant-mode": "dedicated", 

 "oauth2-configuration": { 

"redirect-uris": [ 

  "http://localhost:5000/**", 

  "https://*.cfapps.us10.hana.ondemand.com/**", 

  "https://*.applicationstudio.cloud.sap/**" 

] 

  } 

   

add this in xs app json in app/router 

{ 

  "authenticationMethod":"route", 

  "routes": [ 

{ 

  "source": "^/app/(.*)$", 

  "target": "$1", 

  "localDir": ".", 

  "authenticationType": "xsuaa", 

  "cacheControl": "no-cache, no-store, must-revalidate" 

}, 

{ 

  "source": "^/(.*)$",	 

  "target": "$1", 

  "destination": "srv-api",	 

  "authenticationType": "xsuaa", 

  "csrfProtection": true 

} 

  ] 

} 

 

 

create service xsuaa for application 

cf create-service xsuaa application zentra-auth -c xs-security.json 



create a service key with xsuaa service you have created above 

cf create-service-key zentra-auth zentra_key 

 

bind the service and the key 

cds bind -2 zentra-auth:zentra_key 

 

now run this command to get the credentials and set in default env file 

'cds bind --exec env' copy the vcap for xsuaa 

 

run this command to install libraries inside router nodemodules 

npm install @sap/approuter --prefix app/router 

 

then deploy the application using in cf  

 

then open two terminals 

in first terminal run this cmd 

'cds bind --exec -- npm start --prefix app/router' this should run in 5000 you should now see 'bad gateway' 

in second terminal  

'cds watch --profile hybrid' 

now if u refresh the 5000 port you will redirect to ui page 

 

if it worked 

you have to assign the role to access the data 

go to role collection in subaccount  

create a role collection and add the role you have created in the service layer 

and add user to role collection  

now if you access the data from port 5000 you can able to access it 

-------------------------------------------------------------------- 

https://249d4901trial.authentication.us10.hana.ondemand.com/login 

 



 ------------------------------------- cds cli ----------------------------------------------------
 > cds --help

USAGE
    cds <command> [<args>]
    cds <src>  =  cds compile <src>
    cds        =  cds help

COMMANDS
    i | init        jump-start cds-based projects
    a | add         add a feature to an existing project
    m | import      add models from external sources
    c | compile     compile cds models to different outputs
    p | parse       parses given cds models
    s | serve       run your services in local server
    w | watch       run and restart on file changes
      | mock        call cds serve with mocked service
    r | repl        read-eval-event loop
    e | env         inspect effective configuration
    b | build       prepare for deployment
    d | deploy      deploy to databases
      | up          build and deploy your application to the cloud
    y | bind        bind application to remote services
      | debug       debug your application
      | subscribe   subscribe a tenant to a multitenant SaaS app
      | unsubscribe unsubscribe a tenant from a multitenant SaaS app
    l | login       login to extensible multitenant SaaS app
      | logout      logout from extensible multitenant SaaS app
      | pull        pull base model of extensible SaaS app
      | push        push extension to extensible SaaS app
      | lint        run linter for env or model checks
    v | version     get detailed version information
      | completion  add/remove cli completion for cds commands
    h | help        get detailed usage information

  Learn more about each command using:
  cds help <command> or
  cds <command> --help


use --help for cli ("no one remembers all the cmds" - learned from docker tutorial)
eg:
  cds --help
  cds watch --help
  cf csk --help




Deployment:
	1. Prepare for Production  (once)
	2. Freeze Dependencies  (once: on upgrades)
	3. Build & Assemble
	4. Deploy to Cloud


MTA - multitarget application
	An application comprised of multiple software modules, which are created with different technologies and deployed to different runtimes.

mta.yaml (Development descriptor) → defines what and how to build
mtad.yaml (Deployment descriptor) → defines how to deploy
MTAR: Archive file containing everything needed for deployment
MTAR size: up to 4 GB
mtad.yaml: 1 MB

mta.yaml → written by developer
MBT converts it
mtad.yaml → generated automatically → used by deployment
development descriptor is used to generate deployment descriptor

mta.yaml is officially a development descriptor. It’s sometimes informally called a deployment descriptor because it defines everything needed for deployment, but the actual deployment descriptor is mtad.yaml, which is generated during the build

MTA archive (MTAR):
	Archive containing a deployment descriptor, the module and resource binaries, and configuration files. The archive follows the JAR file specification.

An MTA archive consists of the following:
The MANIFEST.MF file
The mtad.yaml MTA deployment descriptor file
Application binaries, content, or configuration files



cds up - Essentially, this automates the following steps:

# Installing app dependencies, e.g.
npm i app/browse
npm i app/admin-books

# If project is monorepo
ln -sf ../package-lock.json

# If project is multitenant
npm i --package-lock-only --prefix mtx/sidecar

# If package-lock.json doesn't exist
npm i --package-lock-only

# Final assembly and deployment...
mbt build -t gen --mtar mta.tar
cf deploy gen/mta.tar -f

Normally, we run cds add mta manually for MTA deployment. But newer CAP tooling can automatically generate the MTA configuration during cds up if it detects a Cloud Foundry deployment scenario




-----------------------------  Fiori  ------------------------------


manifest.json

1. _version

2. sap.app → App Metadata

3. sap.ui → UI Capabilities

4. sap.ui5 → Main UI Configuration

5. sap.fiori

6. sap.fe

Section	Purpose
flexEnabled
dependencies
contentDensities
models
resources
routing	